# 修改开机动画

开机动画在`/usr/ui/anim`这里，png格式，和手机修改开机动画同理，分辨率为320x480。  

把想要设置为开机动画的图片按源命名方式修改（powerup/powerdown）放在这里就可以了。  

注意由于我们用的是overlayfs，所以实际上并不能做到开机即是修改后的动画，如果想要修改，那就需要我们之前提取出来的分区了。把system分区挂载到电脑上，修改anim图片，再通过fastboot刷回到设备里（只不过这样风险比较高，如果不拆机的话就不太好救砖）。  